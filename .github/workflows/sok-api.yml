name: SOK API

on:
  schedule:
    - cron: '0 4 1 * *'      # 1. i måneden kl. 05: oppdater adresser (slug)
    - cron: '0 5 * * 1'      # hver mandag kl. 06: søk etter nye saker (skann)
  workflow_dispatch:
    inputs:
      modus:
        description: 'Velg: lab / slug / skann'
        required: true
        default: 'skann'
        type: choice
        options:
          - lab
          - slug
          - skann

permissions:
  contents: write

jobs:
  sokapi:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Kjør søk
        env:
          MODUS: ${{ inputs.modus || 'skann' }}
        run: node sok-api.js

      - name: Lagre resultat
        if: ${{ (inputs.modus || 'skann') != 'lab' }}
        run: |
          git config user.name "signal-robot"
          git config user.email "actions@github.com"
          git add acos-slugger.json politikk-acos.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "ACOS-søk $(date +%F)"
          git push
